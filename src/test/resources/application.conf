database {
  mongo {
    uri = "URI"
  }
}

modulesEnabled = "testModule1, testModule2,,testModule3"

jwt {
  algorithm = "HS256"
  secret = "secret"
}

amqp {
  uri = "amqp"
}

tracer {
  client = "off"
}
